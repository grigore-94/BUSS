{% extends '::admin_base.html.twig' %}
{% block body %}
    <script type="text/javascript">
        var lolz;
        function onload() {
            lolz = document.getElementById('lolz');
        }

        function kk() {
            if (lolz.value != "") {
                window.location.assign("http://buss.md/app_dev.php/admin/view/routes/" + lolz.value);
            }else
            { window.location.assign("http://buss.md/app_dev.php/admin/view/routes");}
        }
    </script>

    <body onload="onload();">
    <h2>Filter:</h2>
    <input type="text" name="enter" class="" value="{{ filter }}" id="lolz" style="width:20%"/>

    <i class="fa fa-search" onclick="kk();"></i>

    </body>

    <table class="table table-inverse">
        <thead>
        <tr>
            <th>Operations</th>
            <th>Seats</th>
            <th>Type</th>
            <th>Routes Ho</th>
            <th>Item Routes Ho</th>
        </tr>
        </thead>
        <tbody>
        {% for buss in busses %}
            <tr>
                <td>
                    <a class="fa fa-edit" href="{{ path('edit_buss',{'id':buss.id}) }}"> </a>
                    <a class="fa fa-trash-o" href="{{ path('delete_buss',{'id':buss.id}) }}"
                       onclick="return confirm('Do you want to delete this buss?')"></a>

                </td>
                <td>{{ buss.seats}}</td>
                <td>{{ buss.type }}</td>
                <td>
                    <a class="btn btn-default btn-block" href="#">{{ buss.routes|length }}</a>
                </td>

                <td>
                    <a class="btn btn-default btn-block" href="#">{{ buss.intemRoutes|length }}</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>


{% endblock %}