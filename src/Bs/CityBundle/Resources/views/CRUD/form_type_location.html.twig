{% form_theme form 'bootstrap_3_horizontal_layout.html.twig' %}
{{ form_start(form,{'attr':{'class':'main-login main-center'}}) }}
    {{ form_row(form.city) }}
    {{ form_row(form.region) }}


<button type="submit" class="btn btn-default">Save</button>
{{ form_end(form) }}